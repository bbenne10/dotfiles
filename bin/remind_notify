#!/bin/zsh

NOTIFICATION_STR=$1

if [ -z $NOTIFICATION_STR ]; then
    exit 1
else 
    ~/.bin/pushover_notify {{ PUSHOVER_NOTIFY_APP_KEY }} "$NOTIFICATION_STR" > /dev/null
fi

/usr/bin/notify-send -u critical "Reminder" "$NOTIFICATION_STR"
